[package]
name = "parakeet-audio"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
parakeet-proto.workspace = true
anyhow.workspace = true
serde.workspace = true
serde_json.workspace = true
tokio.workspace = true
tracing.workspace = true
# Used on Windows for WASAPI loopback; compiled everywhere so the backend
# type-checks during Linux development.
cpal = "0.18"

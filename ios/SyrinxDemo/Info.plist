<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>          <string>en</string>
    <key>CFBundleExecutable</key>                 <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>                 <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleName</key>                       <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>                <string>APPL</string>
    <key>CFBundleShortVersionString</key>         <string>0.1</string>
    <key>CFBundleVersion</key>                    <string>1</string>
    <key>LSRequiresIPhoneOS</key>                 <true/>
    <key>UILaunchScreen</key>                     <dict/>
    <!-- Without this the app is killed the moment it touches the microphone. -->
    <key>NSMicrophoneUsageDescription</key>
    <string>Syrinx sends what you say to your own server to be transcribed.</string>
    <!-- Substituted from Local.xcconfig at build time; empty in a clean
         checkout, and the app falls back to its placeholder. -->
    <key>SyrinxURL</key>                          <string>$(SYRINX_URL)</string>
    <key>SyrinxToken</key>                        <string>$(SYRINX_TOKEN)</string>
    <!-- The whole point is dictating into other apps, so capture has to
         survive leaving this one. Without this the session dies on
         backgrounding and the keyboard goes quiet. -->
    <!-- The keyboard extension reaches the app over 127.0.0.1. Loopback is
         not meant to trigger the local-network prompt, but the string has to
         be here in case iOS decides it does; without it the app is killed
         rather than asked about. -->
    <key>NSLocalNetworkUsageDescription</key>
    <string>Syrinx passes transcribed text to its keyboard on this device only.</string>
    <key>UIBackgroundModes</key>
    <array><string>audio</string></array>
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
</dict>
</plist>
